import PageSkeleton from '../components/PageSkeleton';

// Skeleton Ketentuan Layanan - dokumen panjang: judul + section bertingkat.
export default function Loading() {
  return <PageSkeleton variant="doc" />;
}
