import PageSkeleton from '../../components/PageSkeleton';

// Skeleton verifikasi 2FA admin - satu kartu fokus di tengah.
export default function Loading() {
  return <PageSkeleton variant="simple" />;
}
